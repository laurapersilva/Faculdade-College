#include <stdio.h>
#include <stdlib.h>

typedef struct Celula {
    int elemento;
    Celula* prox;
} Celula;

typedef struct Pilha {
    Celula* topo;
}

Pilha * criarPilha(){
    Pilha *Pilha = new malloc()
}

void inserir(Celula * topo) {
    Celula* tmp = topo;
    topo = topo->prox;
    tmp->prox=null;
    tmp = null;
}

int main() {
    Pilha stack = new 
}